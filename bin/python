#!/usr/bin/env bash

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
source /etc/profile.d/105_rcac_work.sh
source $SCRIPT_DIR/../.venv/bin/activate


exec /bin/env python3 "$@"